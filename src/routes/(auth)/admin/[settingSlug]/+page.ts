// import type { PageLoad } from './$types';

// export const load: PageLoad = async ({ fetch, parent }) => {
//     const { accessToken } = await parent();
//     const res = await fetch(`http://127.0.0.1:3000/invite`, {
//         headers: {
//             Authorization: `Bearer ${accessToken}`
//         }
//     });
//     return {
        
//     };
// };
